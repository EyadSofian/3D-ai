/**
 * البرومبت وتقطيع الجمل — مشتركين بين كل مزوّدي الموديل.
 * التقطيع هو اللي بيخلّي الـ TTS يبدأ من أول جملة بدل ما يستنى الرد كله.
 */

const BASE = `أنت "ماجد" — مساعد مصري بيتكلم مع الناس بالصوت، مش بالكتابة.

اللغة: عامية مصرية طبيعية ومحترمة. متكتبش فصحى متكلّفة ولا إنجليزي إلا لو المستخدم كلّمك بيه.

الطول — دي أهم قاعدة: كلامك هيتحوّل لصوت ويتقال بصوت عالي. الردود الطويلة بتبوّظ التجربة.
- رد في جملة أو جملتين. تلاتة أقصى حد لو السؤال محتاج كده فعلًا.
- ادخل في الإجابة على طول. من غير مقدمات زي "سؤال حلو" أو "خليني أوضحلك".
- من غير قوائم، من غير نقط، من غير عناوين، من غير emoji، من غير رموز markdown. نص منطوق بس.
- لو السؤال كبير، جاوب على الجزء الأهم واسأل لو عايز تفاصيل أكتر.

الأرقام والرموز: اكتبها بالحروف زي ما تتنطق ("خمسة وعشرين" مش "25"، "بالمية" مش "%")،
لأن دا بيطلع أنضف بكتير في الصوت.

لو مش عارف حاجة، قولها ببساطة في جملة. متألفش.`;

/** وسوم الأداء — ElevenLabs v3 بس هي اللي بتفهمها. */
const EXPRESSIVE = `

الأداء الصوتي: تقدر تحط وسم أداء بين قوسين مربعين قبل الجملة عشان الصوت يطلع حي:
[يهمس] [يضحك] [متحمس] [مندهش] [بهدوء] [بحماس]
استخدمها بحساب — واحد كل كام جملة لما يكون فيه سبب فعلي، مش في كل رد.
اكتبها لوحدها في أول الجملة، ومتوصفش الانفعال بالكلام كمان.`;

/**
 * @param {boolean} expressive صح لو مزوّد الصوت بيفهم وسوم الأداء
 */
export function systemPrompt(expressive) {
  return expressive ? BASE + EXPRESSIVE : BASE;
}

export const SYSTEM_PROMPT = BASE;

/** علامات نهاية الجملة في العربي والإنجليزي. */
const HARD_STOP = /[.!?؟۔]|[!؟]{1,3}|\n/;
const SOFT_STOP = /[،,؛;:]/;
const MIN_HARD = 10;  // أقل من كده مش جملة، دي كسرة
const MIN_SOFT = 55;  // نقطع على فاصلة بس لو المقطع بقى طويل
const MAX_CHUNK = 220; // مانسمحش لمقطع يكبر أكتر من كده

/**
 * بيقسّم stream من الحروف لمقاطع صالحة للنطق.
 * بيرجّع array بالمقاطع اللي خلصت (ممكن تبقى فاضية).
 */
export function makeSentenceSplitter() {
  let buf = "";
  return {
    push(text) {
      buf += text;
      const out = [];
      let i = 0;
      while (i < buf.length) {
        const ch = buf[i];
        const len = i + 1;
        const hard = HARD_STOP.test(ch) && len >= MIN_HARD;
        const soft = SOFT_STOP.test(ch) && len >= MIN_SOFT;
        if (hard || soft || len >= MAX_CHUNK) {
          // كمّل أي علامات ترقيم ملزوقة (زي "؟!" أو "...")
          let end = i + 1;
          while (end < buf.length && /[.!?؟،,؛;:"'»)\]]/.test(buf[end])) end++;
          const piece = buf.slice(0, end).trim();
          if (piece) out.push(piece);
          buf = buf.slice(end);
          i = 0;
          continue;
        }
        i++;
      }
      return out;
    },
    /** الباقي في الآخر. */
    flush() {
      const rest = buf.trim();
      buf = "";
      return rest ? [rest] : [];
    },
  };
}

